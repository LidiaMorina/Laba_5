package com.company;

import java.util.Set;
import java.util.TreeSet;

/*не работает*/

public interface Comparator<Tariffs> {
  /*  Set<com.company.Tariffs> tariffs = new TreeSet<>(new java.util.Comparator<com.company.Tariffs>() {
        @Override
        public int compare(com.company.Tariffs o1, com.company.Tariffs o2) {
            if (Integer.parseInt(o1.getPayroll()) > Integer.parseInt(o2.getPayroll())) {
                return 1;
            }
            if (Integer.parseInt(o1.getPayroll()) < Integer.parseInt(o2.getPayroll())) {
                return -1;
            } else
                return 0;
        }
    });*/
}
